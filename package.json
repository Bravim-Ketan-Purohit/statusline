{
  "name": "statusline-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test && pnpm bench",
    "bench": "node packages/cli/dist/bench.js",
    "verify": "pnpm build && pnpm test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
